# Frontend para Vercel

Este directorio es el frontend estatico. En Vercel, crea un proyecto usando esta carpeta como **Root Directory**:

```text
frontend-vercel
```

No necesita comando de build.

Antes de publicar, edita `config.js` si tu API no esta en:

```text
https://transcripciones.fly.dev
```
